name: Build Desktop (Godot)

# Compila gli eseguibili desktop (Windows / Linux / macOS) e li pubblica in una
# Release "desktop-latest" sempre aggiornata, da cui scaricarli con un click.
# Parte a ogni aggiornamento di main e si può lanciare a mano da Actions -> Run workflow.

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: "desktop"
  cancel-in-progress: true

env:
  GODOT_VERSION: "4.3-stable"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Scarica Godot headless
        run: |
          curl -sSL -o godot.zip "https://github.com/godotengine/godot/releases/download/${GODOT_VERSION}/Godot_v${GODOT_VERSION}_linux.x86_64.zip"
          unzip -q godot.zip
          sudo mv "Godot_v${GODOT_VERSION}_linux.x86_64" /usr/local/bin/godot
          sudo chmod +x /usr/local/bin/godot

      - name: Installa i template di export
        run: |
          curl -sSL -o templates.tpz "https://github.com/godotengine/godot/releases/download/${GODOT_VERSION}/Godot_v${GODOT_VERSION}_export_templates.tpz"
          mkdir -p "$HOME/.local/share/godot/export_templates/4.3.stable"
          unzip -q templates.tpz
          mv templates/* "$HOME/.local/share/godot/export_templates/4.3.stable/"

      - name: Import del progetto
        run: godot --headless --path godot --import || true

      - name: Test (headless)
        run: godot --headless --path godot -s res://tests/test_runner.gd

      - name: Export desktop (Windows, Linux, macOS)
        run: |
          mkdir -p dist/windows dist/linux dist/macos
          godot --headless --path godot --export-release "Windows Desktop"
          godot --headless --path godot --export-release "Linux"
          godot --headless --path godot --export-release "macOS"

      - name: Export Cuba Libre (stesso motore, game_id=cuba_libre)
        run: |
          sed -i 's|config/name=".*"|config/name="Cuba Libre — Digital"|; s|config/game_id="all_bridges_burning"|config/game_id="cuba_libre"|' godot/project.godot
          mkdir -p dist/cuba
          godot --headless --path godot --export-release "Windows Desktop" ../dist/cuba/CubaLibre.exe
          godot --headless --path godot --export-release "Linux" ../dist/cuba/CubaLibre.x86_64
          godot --headless --path godot --export-release "macOS" ../dist/cuba/CubaLibre.zip
          git checkout -- godot/project.godot

      - name: Impacchetta i file
        run: |
          cd dist
          chmod +x linux/AllBridgesBurning.x86_64 cuba/CubaLibre.x86_64 || true
          zip -j AllBridgesBurning-Windows.zip windows/AllBridgesBurning.exe
          zip -j AllBridgesBurning-Linux.zip linux/AllBridgesBurning.x86_64
          cp macos/AllBridgesBurning.zip AllBridgesBurning-macOS.zip
          zip -j CubaLibre-Windows.zip cuba/CubaLibre.exe
          zip -j CubaLibre-Linux.zip cuba/CubaLibre.x86_64
          cp cuba/CubaLibre.zip CubaLibre-macOS.zip
          ls -lh *.zip

      - name: Pubblica la Release "desktop-latest"
        uses: softprops/action-gh-release@v2
        with:
          tag_name: desktop-latest
          name: "App desktop — ultima versione"
          body: |
            Eseguibili generati automaticamente dall'ultimo aggiornamento del gioco.

            - **Windows**: scarica `AllBridgesBurning-Windows.zip`, estrai, doppio click su `AllBridgesBurning.exe`.
            - **Linux**: scarica `AllBridgesBurning-Linux.zip`, estrai, rendi eseguibile e avvia `AllBridgesBurning.x86_64`.
            - **macOS**: scarica `AllBridgesBurning-macOS.zip`, estrai. L'app non è firmata: al primo avvio fai
              **clic destro → Apri** (oppure Impostazioni → Privacy e sicurezza → Apri comunque).

            Gli zip `CubaLibre-*` sono lo stesso motore con il gioco Cuba Libre (stesse istruzioni di avvio).
          files: |
            dist/AllBridgesBurning-Windows.zip
            dist/AllBridgesBurning-Linux.zip
            dist/AllBridgesBurning-macOS.zip
            dist/CubaLibre-Windows.zip
            dist/CubaLibre-Linux.zip
            dist/CubaLibre-macOS.zip
